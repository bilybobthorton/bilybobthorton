{% layout none %}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>RedGuard — Antivirus + VPN. One Silent App.</title>
  <meta name="description" content="RedGuard combines real-time malware detection and WireGuard VPN into one lightweight Windows app. Start free — 7-day VPN trial included." />
  <link rel="icon" href="https://dashboard.redgaurd.com/favicon.ico" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --red:    #dc2626;
      --red-d:  #b91c1c;
      --bg:     #0a0a0f;
      --bg2:    #0d0d14;
      --bg3:    #111118;
      --border: #1e1e2e;
      --text:   #f1f5f9;
      --muted:  #64748b;
      --sub:    #94a3b8;
    }

    html { scroll-behavior: smooth; }

    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      background: var(--bg);
      color: var(--text);
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
    }

    a { text-decoration: none; color: inherit; }

    /* ── NAV ─────────────────────────────────────────────────────── */
    nav {
      position: sticky; top: 0; z-index: 100;
      display: flex; align-items: center; justify-content: space-between;
      padding: 0 40px; height: 64px;
      background: rgba(10,10,15,0.85);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid var(--border);
    }
    .nav-logo { display: flex; align-items: center; gap: 10px; }
    .nav-logo-icon {
      width: 32px; height: 32px; border-radius: 8px;
      background: var(--red); display: flex; align-items: center; justify-content: center;
    }
    .nav-logo-icon svg { width: 18px; height: 18px; }
    .nav-brand { font-size: 17px; font-weight: 700; letter-spacing: -0.02em; }
    .nav-links { display: flex; align-items: center; gap: 28px; }
    .nav-links a { font-size: 14px; color: var(--sub); transition: color .15s; }
    .nav-links a:hover { color: var(--text); }
    .btn-nav {
      padding: 8px 18px; background: var(--red); color: #fff;
      border-radius: 7px; font-size: 14px; font-weight: 600;
      transition: background .15s;
    }
    .btn-nav:hover { background: var(--red-d); }

    /* ── SECTIONS ─────────────────────────────────────────────────── */
    section { padding: 96px 40px; max-width: 1100px; margin: 0 auto; }
    .section-full { max-width: 100%; padding-left: 0; padding-right: 0; }

    /* ── HERO ─────────────────────────────────────────────────────── */
    .hero {
      text-align: center;
      padding-top: 120px;
      padding-bottom: 80px;
    }
    .hero-badge {
      display: inline-flex; align-items: center; gap: 8px;
      padding: 6px 14px; border-radius: 99px;
      border: 1px solid #7f1d1d; background: #1c0a0a;
      font-size: 12px; font-weight: 600; color: #fca5a5;
      margin-bottom: 28px; letter-spacing: 0.04em; text-transform: uppercase;
    }
    .hero h1 {
      font-size: clamp(36px, 6vw, 72px);
      font-weight: 800;
      letter-spacing: -0.04em;
      line-height: 1.05;
      margin-bottom: 24px;
    }
    .hero h1 span { color: var(--red); }
    .hero p {
      font-size: clamp(16px, 2vw, 20px);
      color: var(--sub);
      max-width: 560px;
      margin: 0 auto 40px;
    }
    .hero-ctas { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; }
    .btn-primary {
      display: inline-flex; align-items: center; gap: 10px;
      padding: 14px 32px;
      background: var(--red); color: #fff;
      border-radius: 10px; font-size: 16px; font-weight: 700;
      transition: background .15s, transform .15s;
      box-shadow: 0 4px 24px rgba(220,38,38,.25);
    }
    .btn-primary:hover { background: var(--red-d); transform: translateY(-1px); }
    .btn-secondary {
      display: inline-flex; align-items: center; gap: 10px;
      padding: 14px 32px;
      background: transparent; color: var(--sub);
      border: 1px solid var(--border); border-radius: 10px;
      font-size: 16px; font-weight: 600;
      transition: border-color .15s, color .15s;
    }
    .btn-secondary:hover { border-color: #334155; color: var(--text); }
    .hero-note { margin-top: 16px; font-size: 13px; color: var(--muted); }

    /* ── STATS BAR ────────────────────────────────────────────────── */
    .stats-bar {
      background: var(--bg2);
      border-top: 1px solid var(--border);
      border-bottom: 1px solid var(--border);
      padding: 32px 40px;
    }
    .stats-inner {
      max-width: 1100px; margin: 0 auto;
      display: grid; grid-template-columns: repeat(4, 1fr);
      gap: 24px; text-align: center;
    }
    .stat-num {
      font-size: 32px; font-weight: 800;
      letter-spacing: -0.03em; color: var(--text);
      margin-bottom: 4px;
    }
    .stat-num span { color: var(--red); }
    .stat-label { font-size: 13px; color: var(--muted); }

    /* ── HOW IT WORKS ─────────────────────────────────────────────── */
    .section-label {
      font-size: 12px; font-weight: 700; letter-spacing: 0.1em;
      text-transform: uppercase; color: var(--red);
      margin-bottom: 12px;
    }
    .section-title {
      font-size: clamp(28px, 4vw, 42px);
      font-weight: 800; letter-spacing: -0.03em;
      margin-bottom: 16px;
    }
    .section-sub { font-size: 16px; color: var(--sub); max-width: 520px; }

    .steps { display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px; margin-top: 56px; }
    .step {
      background: var(--bg2); border: 1px solid var(--border);
      border-radius: 14px; padding: 28px;
    }
    .step-num {
      width: 36px; height: 36px; border-radius: 50%;
      background: #1c0a0a; border: 1px solid #7f1d1d;
      color: #fca5a5; font-size: 14px; font-weight: 800;
      display: flex; align-items: center; justify-content: center;
      margin-bottom: 16px;
    }
    .step h3 { font-size: 17px; font-weight: 700; margin-bottom: 8px; }
    .step p { font-size: 14px; color: var(--sub); line-height: 1.6; }

    /* ── FEATURES ─────────────────────────────────────────────────── */
    .features-grid {
      display: grid; grid-template-columns: repeat(3, 1fr);
      gap: 20px; margin-top: 56px;
    }
    .feature-card {
      background: var(--bg2); border: 1px solid var(--border);
      border-radius: 14px; padding: 24px;
      transition: border-color .2s;
    }
    .feature-card:hover { border-color: #374151; }
    .feature-icon {
      width: 40px; height: 40px; border-radius: 10px;
      background: #1c0a0a; border: 1px solid #7f1d1d;
      display: flex; align-items: center; justify-content: center;
      margin-bottom: 14px;
    }
    .feature-icon svg { width: 20px; height: 20px; color: #fca5a5; }
    .feature-card h3 { font-size: 15px; font-weight: 700; margin-bottom: 6px; }
    .feature-card p { font-size: 13px; color: var(--sub); line-height: 1.6; }

    /* ── PRICING ──────────────────────────────────────────────────── */
    .pricing-grid {
      display: grid; grid-template-columns: repeat(3, 1fr);
      gap: 20px; margin-top: 56px; align-items: start;
    }
    .pricing-card {
      background: var(--bg2); border: 1px solid var(--border);
      border-radius: 16px; padding: 28px; position: relative;
    }
    .pricing-card.featured {
      border-color: var(--red);
      background: linear-gradient(160deg, #1c0a0a 0%, var(--bg2) 60%);
    }
    .pricing-badge {
      position: absolute; top: -12px; left: 50%; transform: translateX(-50%);
      padding: 4px 14px; border-radius: 99px;
      background: var(--red); color: #fff;
      font-size: 11px; font-weight: 700; letter-spacing: 0.06em;
      text-transform: uppercase; white-space: nowrap;
    }
    .pricing-name { font-size: 14px; font-weight: 700; color: var(--sub); text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 8px; }
    .pricing-price {
      font-size: 42px; font-weight: 800; letter-spacing: -0.04em;
      color: var(--text); margin-bottom: 4px;
    }
    .pricing-price sup { font-size: 20px; vertical-align: super; }
    .pricing-price span { font-size: 16px; font-weight: 400; color: var(--muted); }
    .pricing-desc { font-size: 13px; color: var(--sub); margin-bottom: 24px; min-height: 40px; }
    .pricing-features { list-style: none; margin-bottom: 28px; display: flex; flex-direction: column; gap: 10px; }
    .pricing-features li { display: flex; align-items: center; gap: 10px; font-size: 14px; color: var(--sub); }
    .pricing-features li::before { content: "✓"; color: #22c55e; font-weight: 700; flex-shrink: 0; }
    .pricing-features li.no::before { content: "✕"; color: var(--muted); }
    .pricing-features li.no { color: var(--muted); }
    .btn-plan {
      display: block; width: 100%; text-align: center;
      padding: 12px; border-radius: 9px; font-size: 15px; font-weight: 700;
      transition: all .15s;
    }
    .btn-plan-primary { background: var(--red); color: #fff; }
    .btn-plan-primary:hover { background: var(--red-d); }
    .btn-plan-outline {
      background: transparent; color: var(--sub);
      border: 1px solid var(--border);
    }
    .btn-plan-outline:hover { border-color: #334155; color: var(--text); }

    /* ── DOWNLOAD CTA ─────────────────────────────────────────────── */
    .download-cta {
      background: var(--bg2);
      border-top: 1px solid var(--border);
      border-bottom: 1px solid var(--border);
      text-align: center;
      padding: 96px 40px;
    }
    .download-cta h2 {
      font-size: clamp(28px, 4vw, 48px);
      font-weight: 800; letter-spacing: -0.03em; margin-bottom: 16px;
    }
    .download-cta p { font-size: 16px; color: var(--sub); margin-bottom: 40px; }

    /* ── FOOTER ───────────────────────────────────────────────────── */
    footer {
      padding: 48px 40px; max-width: 1100px; margin: 0 auto;
      display: flex; align-items: center; justify-content: space-between;
      border-top: 1px solid var(--border);
      flex-wrap: wrap; gap: 16px;
    }
    footer p { font-size: 13px; color: var(--muted); }
    .footer-links { display: flex; gap: 24px; }
    .footer-links a { font-size: 13px; color: var(--muted); transition: color .15s; }
    .footer-links a:hover { color: var(--sub); }

    /* ── RESPONSIVE ───────────────────────────────────────────────── */
    @media (max-width: 768px) {
      nav { padding: 0 20px; }
      .nav-links { display: none; }
      section { padding: 64px 20px; }
      .stats-inner { grid-template-columns: repeat(2, 1fr); }
      .steps, .features-grid, .pricing-grid { grid-template-columns: 1fr; }
      footer { flex-direction: column; text-align: center; }
    }
  </style>
</head>
<body>

<!-- NAV -->
<nav>
  <a href="/" class="nav-logo">
    <div class="nav-logo-icon">
      <svg fill="none" viewBox="0 0 24 24" stroke="white" stroke-width="2.5">
        <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75m-3-7.036A11.959 11.959 0 013.598 6 11.99 11.99 0 003 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285z"/>
      </svg>
    </div>
    <span class="nav-brand">RedGuard</span>
  </a>
  <div class="nav-links">
    <a href="https://dashboard.redgaurd.com/download">Download</a>
    <a href="#features">Features</a>
    <a href="#pricing">Pricing</a>
    <a href="https://dashboard.redgaurd.com/login">Sign in</a>
    <a href="https://dashboard.redgaurd.com/register" class="btn-nav">Start free</a>
  </div>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="hero-badge">
    <svg width="12" height="12" viewBox="0 0 24 24" fill="currentColor"><circle cx="12" cy="12" r="10"/></svg>
    Free 7-day VPN trial — no credit card
  </div>
  <h1>Real antivirus.<br><span>Real VPN.</span><br>One silent app.</h1>
  <p>RedGuard runs quietly in the background — scanning threats and encrypting your traffic with military-grade WireGuard encryption.</p>
  <div class="hero-ctas">
    <a href="https://dashboard.redgaurd.com/register" class="btn-primary">
      <svg width="18" height="18" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"/></svg>
      Start free trial
    </a>
    <a href="https://dashboard.redgaurd.com/download" class="btn-secondary">
      Download for Windows →
    </a>
  </div>
  <p class="hero-note">Windows 10 / 11 · 64-bit · ~15 MB · No credit card required</p>
</section>

<!-- STATS BAR -->
<div class="stats-bar">
  <div class="stats-inner">
    <div>
      <div class="stat-num">2.4<span>M+</span></div>
      <div class="stat-label">Files scanned</div>
    </div>
    <div>
      <div class="stat-num">18<span>K+</span></div>
      <div class="stat-label">Threats blocked</div>
    </div>
    <div>
      <div class="stat-num">99.<span>9%</span></div>
      <div class="stat-label">Uptime SLA</div>
    </div>
    <div>
      <div class="stat-num"><span>&lt;</span>1ms</div>
      <div class="stat-label">VPN latency overhead</div>
    </div>
  </div>
</div>

<!-- HOW IT WORKS -->
<section>
  <div class="section-label">How it works</div>
  <div class="section-title">Up and running in 60 seconds</div>
  <p class="section-sub">Download once. Sign in once. RedGuard handles everything else silently.</p>

  <div class="steps">
    <div class="step">
      <div class="step-num">1</div>
      <h3>Download the app</h3>
      <p>Install RedGuard on your Windows PC. The setup takes under a minute and requires no technical knowledge.</p>
    </div>
    <div class="step">
      <div class="step-num">2</div>
      <h3>Create a free account</h3>
      <p>Sign up with your email. Your 7-day VPN trial starts immediately — no credit card needed.</p>
    </div>
    <div class="step">
      <div class="step-num">3</div>
      <h3>Protection starts instantly</h3>
      <p>Flip the shield on. RedGuard scans threats in real time and encrypts your connection in the background.</p>
    </div>
  </div>
</section>

<!-- FEATURES -->
<section id="features" style="background: var(--bg3); max-width: 100%; padding-left: 0; padding-right: 0;">
  <div style="max-width: 1100px; margin: 0 auto; padding: 96px 40px;">
    <div class="section-label">Features</div>
    <div class="section-title">Everything you need. Nothing you don't.</div>
    <p class="section-sub">Built for people who want real protection without complexity.</p>

    <div class="features-grid">
      <div class="feature-card">
        <div class="feature-icon">
          <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75m-3-7.036A11.959 11.959 0 013.598 6 11.99 11.99 0 003 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285z"/></svg>
        </div>
        <h3>Real-time malware detection</h3>
        <p>AI-powered scanning catches known and unknown threats before they execute. Updated threat signatures from the cloud.</p>
      </div>
      <div class="feature-card">
        <div class="feature-icon">
          <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/></svg>
        </div>
        <h3>WireGuard VPN</h3>
        <p>The fastest VPN protocol available. Your traffic is encrypted end-to-end through our private server network. Zero logs.</p>
      </div>
      <div class="feature-card">
        <div class="feature-icon">
          <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/><path stroke-linecap="round" stroke-linejoin="round" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/></svg>
        </div>
        <h3>Silent background protection</h3>
        <p>Runs invisibly in your system tray. Uses less than 1% CPU. You never know it's there until it stops a threat.</p>
      </div>
      <div class="feature-card">
        <div class="feature-icon">
          <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/></svg>
        </div>
        <h3>Full system scan</h3>
        <p>Scan every executable across your entire drive — Downloads, Program Files, AppData. Hash-checked against our threat database.</p>
      </div>
      <div class="feature-card">
        <div class="feature-icon">
          <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
        </div>
        <h3>Instant threat response</h3>
        <p>Suspicious files are quarantined immediately. Get notified the second something tries to run on your machine.</p>
      </div>
      <div class="feature-card">
        <div class="feature-icon">
          <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M3 15a4 4 0 004 4h9a5 5 0 10-.1-9.999 5.002 5.002 0 10-9.78 2.096A4.001 4.001 0 003 15z"/></svg>
        </div>
        <h3>Cloud threat intelligence</h3>
        <p>Every scan is backed by VirusTotal, AlienVault OTX, and our proprietary ML model trained on millions of samples.</p>
      </div>
    </div>
  </div>
</section>

<!-- PRICING -->
<section id="pricing">
  <div style="text-align: center; margin-bottom: 8px;">
    <div class="section-label" style="display: inline-block;">Pricing</div>
  </div>
  <div class="section-title" style="text-align: center;">Start free. Upgrade when you're ready.</div>
  <p class="section-sub" style="text-align: center; margin: 0 auto;">No contracts. Cancel anytime. Plans billed monthly.</p>

  <div class="pricing-grid">

    <!-- FREE -->
    <div class="pricing-card">
      <div class="pricing-name">Free</div>
      <div class="pricing-price">$0</div>
      <div class="pricing-desc">Try RedGuard free. Includes a 7-day VPN trial to see what you're missing.</div>
      <ul class="pricing-features">
        <li>Basic malware scanning (5/day)</li>
        <li>Local hash reputation check</li>
        <li>7-day VPN trial included</li>
        <li>1 device</li>
        <li class="no">Unlimited scans</li>
        <li class="no">ML threat scoring</li>
        <li class="no">VPN after trial</li>
      </ul>
      <a href="https://dashboard.redgaurd.com/register" class="btn-plan btn-plan-outline">
        Start free trial
      </a>
    </div>

    <!-- PRO -->
    <div class="pricing-card featured">
      <div class="pricing-badge">Most popular</div>
      <div class="pricing-name">Security Bundle</div>
      <div class="pricing-price"><sup>$</sup>12<span>.99/mo</span></div>
      <div class="pricing-desc">Full antivirus + VPN in one subscription. Best value — 30% off vs separate.</div>
      <ul class="pricing-features">
        <li>Unlimited malware scans</li>
        <li>AI / ML threat scoring</li>
        <li>Custom YARA rules</li>
        <li>VPN Pro — unlimited bandwidth</li>
        <li>All server locations</li>
        <li>5 devices</li>
        <li>Priority support</li>
      </ul>
      <a href="/products/security-bundle" class="btn-plan btn-plan-primary">
        Get the Bundle
      </a>
    </div>

    <!-- ANTIVIRUS PRO -->
    <div class="pricing-card">
      <div class="pricing-name">Antivirus Pro</div>
      <div class="pricing-price"><sup>$</sup>9<span>.99/mo</span></div>
      <div class="pricing-desc">Full malware protection without the VPN. For users who already have a VPN.</div>
      <ul class="pricing-features">
        <li>Unlimited malware scans</li>
        <li>AI / ML threat scoring</li>
        <li>Custom YARA rules</li>
        <li>PDF threat reports</li>
        <li>Email alerts</li>
        <li class="no">VPN included</li>
        <li class="no">Multi-device VPN</li>
      </ul>
      <a href="/products/redguard-pro" class="btn-plan btn-plan-outline">
        Get Antivirus Pro
      </a>
    </div>

  </div>

  <p style="text-align: center; font-size: 13px; color: var(--muted); margin-top: 24px;">
    Need enterprise? <a href="mailto:enterprise@redgaurd.com" style="color: var(--sub);">Contact us</a> for custom pricing, dedicated nodes, and SLA.
  </p>
</section>

<!-- DOWNLOAD CTA -->
<div class="download-cta">
  <h2>Ready to stop threats<br>before they start?</h2>
  <p>Download RedGuard and get your free 7-day VPN trial today.<br>No credit card. No setup. No nonsense.</p>
  <div style="display: flex; gap: 16px; justify-content: center; flex-wrap: wrap;">
    <a href="https://dashboard.redgaurd.com/register" class="btn-primary" style="font-size: 17px; padding: 16px 36px;">
      Start free — takes 60 seconds
    </a>
    <a href="https://dashboard.redgaurd.com/download" class="btn-secondary" style="font-size: 17px; padding: 16px 36px;">
      Download the app →
    </a>
  </div>
</div>

<!-- FOOTER -->
<footer>
  <div>
    <div class="nav-logo" style="margin-bottom: 8px;">
      <div class="nav-logo-icon" style="width: 24px; height: 24px; border-radius: 6px;">
        <svg width="14" height="14" fill="none" viewBox="0 0 24 24" stroke="white" stroke-width="2.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75m-3-7.036A11.959 11.959 0 013.598 6 11.99 11.99 0 003 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285z"/>
        </svg>
      </div>
      <span style="font-size: 15px; font-weight: 700;">RedGuard</span>
    </div>
    <p>© 2026 RedGuard. Built by the people, for the people.</p>
  </div>
  <div class="footer-links">
    <a href="https://dashboard.redgaurd.com/download">Download</a>
    <a href="https://dashboard.redgaurd.com/login">Sign in</a>
    <a href="https://dashboard.redgaurd.com/register">Register</a>
    <a href="mailto:support@redgaurd.com">Support</a>
    <a href="/pages/privacy-policy">Privacy</a>
    <a href="/pages/terms">Terms</a>
  </div>
</footer>

</body>
</html>
